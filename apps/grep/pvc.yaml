apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gitrepo
  namespace: apps--grep
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 3Gi
